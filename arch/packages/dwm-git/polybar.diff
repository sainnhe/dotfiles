diff --git a/config.h b/config.h
index 747d9a0..9a21a4c 100644
--- a/config.h
+++ b/config.h
@@ -15,6 +15,7 @@ static const int swallowfloating    = 0;        /* 1 means swallow floating wind
 static const int smartgaps          = 0;        /* 1 means no outer gap when there is only one window */
 static const int showbar            = 1;        /* 0 means no bar */
 static const int topbar             = 1;        /* 0 means bottom bar */
+static const int yoffset            = 0;        /* y-offset */
 static const char *fonts[]          = { "monospace:size=10", "JoyPixels:pixelsize=10:antialias=true:autohint=true"  };
 static char dmenufont[]             = "monospace:size=10";
 static char normbgcolor[]           = "#222222";
diff --git a/dwm.c b/dwm.c
index ff6ef46..7c76f05 100644
--- a/dwm.c
+++ b/dwm.c
@@ -2194,8 +2194,10 @@ updatebarpos(Monitor *m)
 		m->wh -= bh;
 		m->by = m->topbar ? m->wy : m->wy + m->wh;
 		m->wy = m->topbar ? m->wy + bh : m->wy;
-	} else
+	} else {
 		m->by = -bh;
+	}
+	m->wh -= yoffset;
 }
 
 void
